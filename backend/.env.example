PORT=5000
NODE_ENV=development

# PostgreSQL - use DATABASE_URL or individual variables
DATABASE_URL=
# Or use individual variables:
PGHOST=localhost
PGPORT=5432
PGDATABASE=wwenatou
PGUSER=
PGPASSWORD=

# JWT (app-level authentication)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# CORS origin for production (optional, defaults to same-origin)
CORS_ORIGIN=
